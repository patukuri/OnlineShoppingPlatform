package com.securecoding.onelineshoppingplatform.repository;

/*
 * import java.util.List;
 * 
 * import org.springframework.data.jpa.repository.JpaRepository; import
 * org.springframework.data.jpa.repository.Query; import
 * org.springframework.stereotype.Repository;
 * 
 * import com.securecoding.onelineshoppingplatform.Model.Product;
 * 
 * public interface GetAllProductsRepo extends JpaRepository<Product, Integer> {
 * 
 * @Query("SELECT p FROM products p WHERE " +
 * "p.name LIKE CONCAT('%',:query, '%')") List<Product>searchProducts(String
 * name);
 * 
 * }
 */